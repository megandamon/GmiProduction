module purge
module load comp/intel-9.1.042
module load mpi/scali-5

setenv ARCHITECTURE ARCH_INTEL

if (-e ~/.cshrc.gmi) then
   source ~/.cshrc.gmi
endif

if (-e ~/.login.gmi) then
   source ~/.login.gmi
endif
